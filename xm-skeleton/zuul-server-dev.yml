spring:
  application:
    name: zuul-server
server:
  port: 4001
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1001/eureka/
zuul:
  routes:
    api-a:
      path: /api-a/**
      serviceId: order-server